<ul>
    {{#each posts as |post|}}
    <li>
        <a href="/post/{{post.id}}">
            <h2>{{post.title}}</h2>
            <p>Posted by {{post.user.username}}</p>
        </a>
    </li>
    {{/each}}
</ul>