<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Dev Dash</title>
    <link rel="stylesheet" href="/css/style.css">
  </head>

  <body>
    <header class="header-container">
      <div class="header-title">
        <h1>Dev Dash</h1>
        <p>tech, code, computer science blog</p>
      </div>
      <div class="header-nav">
        {{#if loggedIn}}
        <p class="header-username">Welcome( <span class="username">{{username}}</span> )</p>
        {{else}}
        <p class="header-username">
          <br />
        </p>
        {{/if}}
        <nav class="nav-bar">

          {{#if isHome}}
          <span class="nav-location no-bar">Home</span>
          {{else}}
          <a href="/" class="nav-link no-bar">Home</a>
          {{/if}}

          {{#if loggedIn}}
            {{#if isDash}}
            <span class="nav-location">Dashboard</span>
            {{else}}
            <a href="/dashboard" class="nav-link">Dashboard</a>
            {{/if}}
            <button id="logout" class="nav-btn">Logout</button>
          {{else}}
            {{#if isLogin}}
            <span class="nav-location">Login</span>
            {{else}}
            <a href="/login" class="nav-link">Login</a>
            {{/if}}
            {{#if isSignup}}
            <span class="nav-location">Sign Up</span>
            {{else}}
            <a href="/signup" class="nav-link">Sign Up</a>
            {{/if}}
          {{/if}}
        </nav>
      </div>
    </header>

    <main>
    {{{body}}}
    </main>

    <footer>
      <p>
        Thanks for visiting!
      </p>
    </footer>

    {{#if loggedIn}}
    <script src="/js/logout.js"></script>
    {{/if}}
  </body>
</html>
