{{#each this}}
  <div class="comment-item">
    <div class="comment-item-text">
      <p>{{text}}</p>
    </div>
    <div class="comment-item-footer">
      <span class="username">{{user.username}}</span> ({{format_date created_at}})
    </div>
  </div>
{{/each}}